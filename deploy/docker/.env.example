# E2EE WebRTC Production Configuration
# Copy this file to .env and fill in your values

# Domain Configuration
DOMAIN=example.com
ACME_EMAIL=admin@example.com

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=https://example.com,https://www.example.com

# JWT Secret for authentication (generate with: openssl rand -hex 32)
JWT_SECRET=your-secure-jwt-secret-change-me

# Traefik Dashboard Auth (generate with: htpasswd -nb admin password)
TRAEFIK_USERS=admin:$$apr1$$xyz...

# TURN Server Credentials
TURN_USER=e2ee-turn
TURN_PASSWORD=your-secure-turn-password

# TURN Server Secret (for dynamic credentials, generate with: openssl rand -hex 32)
TURN_SECRET=your-turn-shared-secret
